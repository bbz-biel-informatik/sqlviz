<% visual.datasets.each do |dataset| %>
  <table>
    <tr><%= dataset.data.columns.map{ |c| "<th>#{c}</th>" }.join.html_safe %></tr>
    <% dataset.data.rows.each do |row| %>
      <tr><%= row.map{ |c| "<td>#{c}</td>" }.join.html_safe %></tr>
    <% end %>
  </table>
<% end %>
